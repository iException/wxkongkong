<template name="applicantorview">
    <view class="applicantorview">
        <view class="userview">
            <image class="avatar" src="{{avatar}}"></image>
            <view class="descview">
                <text class="namelbl">{{name}}</text>
                <text class="datelbl">{{date}}</text>
            </view>
        </view>
        <view class="reasonview">{{reason}}</view>
        <view class="dottedline"></view>
    </view>
</template>